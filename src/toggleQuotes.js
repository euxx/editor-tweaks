'use strict';

// TODO: Implement Toggle Quotes feature
// Cycles the quote character surrounding the cursor: " -> ' -> ` -> "
// Reference: britesnow.vscode-toggle-quotes (last updated 2019, no longer maintained)

/**
 * @param {import('vscode').ExtensionContext} _context
 */
function activate(_context) {
  // TODO: register command 'editorTweaks.toggleQuotes'
}

function deactivate() {}

module.exports = { activate, deactivate };
